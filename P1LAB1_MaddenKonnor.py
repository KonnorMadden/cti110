# taking the first name input from the user
first_name = input()

# printing the welcome message to the user using f-string to print the name in the message that is inputted by the user and not printing a hard-coded name
# f-string helps to add variables inside the string itself using curly braces {}
# we will insert the variable first_name in our f-string

print(f'Hello {first_name} and welcome to CS Online!')